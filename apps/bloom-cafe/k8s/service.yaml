apiVersion: v1
kind: Service
metadata:
  name: bloom-cafe
  namespace: sites-dev
  labels:
    app: bloom-cafe
spec:
  selector:
    app: bloom-cafe
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP