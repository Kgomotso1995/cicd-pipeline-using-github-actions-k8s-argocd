apiVersion: v1
kind: Service
metadata:
  name: arclight-agency
  namespace: sites-dev
  labels:
    app: arclight-agency
    env: dev
spec:
  type: ClusterIP
  selector:
    app: arclight-agency
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
