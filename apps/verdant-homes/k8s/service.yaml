apiVersion: v1
kind: Service
metadata:
  name: verdant-homes
  namespace: sites-dev
  labels:
    app: verdant-homes
    env: dev
spec:
  type: ClusterIP
  selector:
    app: verdant-homes
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
