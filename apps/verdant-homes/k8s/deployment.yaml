apiVersion: apps/v1
kind: Deployment
metadata:
  name: verdant-homes
  namespace: staging
  labels:
    app: verdant-homes
    env: dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: verdant-homes
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: verdant-homes
        env: dev
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: verdant-homes
          image: ghcr.io/kgomotso1995/verdant_homes:staging
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "50m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 101
            allowPrivilegeEscalation: false
