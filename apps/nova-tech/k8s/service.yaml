apiVersion: v1
kind: Service
metadata:
  name: nova-tech
  namespace: sites-dev
  labels:
    app: nova-tech
    env: dev
spec:
  type: ClusterIP
  selector:
    app: nova-tech
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
