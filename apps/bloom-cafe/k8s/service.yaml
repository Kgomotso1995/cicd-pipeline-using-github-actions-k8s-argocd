apiVersion: v1
kind: Service
metadata:
  name: bloom-cafe
  namespace: sites-dev
  labels:
    app: bloom-cafe
    env: dev
spec:
  type: ClusterIP
  selector:
    app: bloom-cafe
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
