apiVersion: v1
kind: Service
metadata:
  name: zenfit-studio
  namespace: sites-dev
  labels:
    app: zenfit-studio
    env: dev
spec:
  type: ClusterIP
  selector:
    app: zenfit-studio
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
