apiVersion: v1
kind: Service
metadata:
  name: arclight-agency-service
  labels:
    app: arclight-agency

spec:
  type: ClusterIP

  selector:
    app: arclight-agency

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80