apiVersion: v1
kind: Service
metadata:
  name: zenfit-studio
  namespace: sites-dev
  labels:
    app: zenfit-studio
spec:
  selector:
    app: zenfit-studio
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP