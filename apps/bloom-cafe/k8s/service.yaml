apiVersion: v1
kind: Service
metadata:
  name: bloom-cafe-service
  labels:
    app: bloom-cafe

spec:
  type: ClusterIP

  selector:
    app: bloom-cafe

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80