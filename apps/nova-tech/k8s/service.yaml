apiVersion: v1
kind: Service
metadata:
  name: nova-tech-service
  labels:
    app: nova-tech

spec:
  type: ClusterIP

  selector:
    app: nova-tech

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80